{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "clean": "rm -rf packages/*/dist",
    "docs": "npx typedoc",
    "test": "npm run build && cd ${LLOYAL_NODE:-../lloyal-node} && npm link $INIT_CWD/packages/sdk $INIT_CWD/packages/agents && npm run build:ts && npm run test:integration; npm install --ignore-scripts"
  },
  "devDependencies": {
    "@lloyal-labs/lloyal.node": "^2.0.0",
    "@types/node": "^25.3.0",
    "tsx": "^4.21.0",
    "typedoc": "^0.28.0",
    "typedoc-rhineai-theme": "^1.0.0",
    "typescript": "^5.9.3"
  }
}
